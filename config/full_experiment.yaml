# config/full_experiment.yaml
# Full-scale configuration mirroring the original experimental setup.
train:
  epochs: 50
  batch_size: 128
  learning_rate: 1e-4

data:
  subset: null  # use the entire dataset

model:
  hidden_size: 512
