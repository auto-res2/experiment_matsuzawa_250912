# config/full_experiment.yaml
train:
  epochs: 10   # still moderate to keep CI time reasonable
  batch_size: 64
  learning_rate: 1e-3

data:
  subset: 2000  # larger synthetic dataset

model:
  hidden_size: 128
