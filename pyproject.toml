[project]
name = "cipher_omega"
version = "0.1.0"
requires-python = ">=3.9"
readme = "README.md"
license = { text = "Apache-2.0" }
authors = [
  { name = "Researcher", email = "research@example.com" }
]

dependencies = [
  "torch>=2.0.0",              # new major version required by transformers 4.56
  "torchvision>=0.15.0",
  "transformers>=4.28.0",
  "datasets>=2.10.0",
  "tqdm>=4.64.0",
  "requests>=2.25.0",
  "PyYAML>=6.0",
  "matplotlib>=3.5.0"
]

