[project]
name = "tiger-lite-cl"
version = "0.1.0"
requires-python = ">=3.9"
description = "TIGER-Lite continual-learning experiments"

# -----------------------------------------------------------------------------
#   Dependencies – MUST be an array, not a mapping
# -----------------------------------------------------------------------------

dependencies = [
    "torch>=1.9.0",
    "torchvision>=0.10.0",
    "psutil>=5.9.0",
    "pyyaml>=6.0",
    "matplotlib>=3.5.0",
    "seaborn>=0.12.0",
]
