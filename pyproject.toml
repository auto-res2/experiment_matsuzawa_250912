[project]
name = "cipher_omega"
version = "0.1.0"
requires-python = ">=3.9"

dependencies = [
  "pyyaml>=6.0",
  "tqdm>=4.64",
  "requests>=2.31",
  "matplotlib>=3.6"
]

[build-system]
requires = ["setuptools>=65.0", "wheel"]
build-backend = "setuptools.build_meta"
